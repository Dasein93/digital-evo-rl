# Prompts (Gemini / Jules)

## Scaffold (Phase 1)
Create a Python 3.12 codebase for a multi-agent predator–prey gridworld using PettingZoo + Gymnasium, PPO baseline in PyTorch, YAML configs, CSV/PNG logging, and a script `run_cpu.py` for quick tests. Output each file with paths.

## Fix error
I ran the code in Google Colab and got this traceback: <paste>. Explain the cause and give exact code changes to fix it.

## Add feature
Add a trajectory recorder (JSONL + NPZ) with `sample_rate`, and write a CLI `tools/replay.py` to export a short MP4 from a recorded trajectory.
